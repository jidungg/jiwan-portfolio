---
id: build-verification-skill
title: 빌드 검증 스킬 — AI가 팀 공용 문서에 적어버린 개인 엔진 경로 걷어내기
summary: "커밋 전 빌드 통과라는 팀 규칙을 AI 에이전트가 자동으로 지키게 만든 스킬. AI가 제 엔진 경로를 팀 공용 CLAUDE.md에 하드코딩한 것을 보고, 경로 해석을 인자→ini→.uproject→레지스트리 4단계로 스킬 안에 옮겨 팀원 누구나 무설정으로 쓰게 만들었습니다"
project: Dogong (팀 프로젝트, Untitled-Forge)
role: 클라이언트 프로그래머 (팀 프로젝트). 이 사례의 Git 워크플로우 문서·브랜치 작업 스킬·빌드 검증 스킬은 단독 작성
period: 2026-04 ~ 2026-08
engine: [Unreal 5.8]
lang: [PowerShell, C++]
domain: [툴, 프로세스]
skills: [빌드 자동화, 개발 워크플로우 설계, 생성형 AI 활용, PowerShell, Git 워크플로우, 협업 문서화]
sources:
  - Sources/Projects.md — Dogong (.claude/skills/build-test 의 build.ps1·SKILL.md, .claude/skills/finish-feature, .claude/rules/gitcommit-rules.md·GitWorkflow.md)
  - 커밋 1732d2ed (build-test 빌드 검증 스킬 추가)
  - 커밋 d96c9b7a (UE MCP 툴셋 플러그인 활성화 및 Claude Code 서버 설정 추가)
  - 커밋 e000c643 (에디터 Python 원격 실행 활성화)
  - 커밋 3ae30748 (도메인 언어 glossary 추가)
---

## 배경

Dogong은 팀 프로젝트이고, 저는 여기서 **Git 워크플로우 문서와 브랜치 작업 스킬을
맡아 정리했습니다.** 그 과정에서 규칙을 하나 세웠습니다 — *WIP 커밋이라도 빌드가
깨지는 상태로 push 하지 않는다.*

규칙을 문서에 적어두는 것만으로는 잘 안 지켜집니다. 그래서 브랜치를 PR로 마무리하는
`finish-feature` 스킬을 쓸 때 빌드 테스트가 자동으로 돌게 만들려고 했습니다. 그런데
막상 시켜보니 두 가지가 걸렸습니다.

**첫째, 엔진 디렉토리가 어딘지 몰라서 한참을 찾았습니다.** 언리얼 프로젝트를
빌드하려면 엔진의 `Build.bat` 위치를 알아야 하는데, 그 경로는 프로젝트 안에
적혀 있지 않습니다.

**둘째, 찾고 나서는 제 엔진 경로를 팀원들이 공통으로 쓰는 `CLAUDE.md`에
적어놨습니다.** 제 PC에서는 잘 돌아갑니다. 하지만 그 파일은 팀원 전체가 받아 쓰는
파일이고, 팀원 입장에서는 존재하지도 않는 경로를 가리키게 됩니다. 게다가 저는
소스빌드 엔진을 쓰고 팀원 대부분은 런처에서 설치한 엔진을 쓰기 때문에, 경로 하나를
적어두는 방식으로는 애초에 맞출 수가 없습니다.

**다른 팀원들은 바로 쓸 수 없겠다 싶어서**, 경로를 문서에 적는 대신 스킬이 스스로
찾아내게 만들기로 했습니다.

## 구현

`build-test` 스킬은 두 부분입니다. AI가 읽는 사용 지침(`SKILL.md`)과 실제로 빌드를
돌리는 `build.ps1`입니다. 핵심은 스크립트 쪽의 **엔진 경로 자동 해석**입니다.

우선순위를 네 단계로 두고, 앞 단계가 실패하면 다음으로 넘어갑니다.

```powershell
# 1) -EnginePath 인자  2) Developers/BuildConfig.ini  3) .uproject → 레지스트리
$iniVal = Read-IniEnginePath -Path $IniPath
if ($iniVal) { $iniVal = $iniVal.TrimEnd('\', '/') }

if ($iniVal -and (Test-Path (Join-Path $iniVal "Engine\Build\BatchFiles\Build.bat"))) {
    $Engine = $iniVal; $engineSource = "BuildConfig.ini"
}
else {
    $assoc = (Get-Content $ProjPath -Raw | ConvertFrom-Json).EngineAssociation
    $resolved = Resolve-FromRegistry -Association $assoc   # 숫자면 런처, 이름이면 소스빌드
    if ($resolved) {
        $Engine = $resolved
        if ($iniVal -ne $Engine) { Write-IniEnginePath -Path $IniPath -Value $Engine }
    }
}
# 4) 모두 실패 → 빈 EnginePath 로 ini 를 만들어주고 exit 3 으로 안내
```

3단계가 팀원 환경을 흡수하는 자리입니다. `.uproject`의 `EngineAssociation` 값이
숫자 버전(`"5.8"`)이면 런처로 설치한 엔진이므로 `HKLM`의 `InstalledDirectory`를
보고, 이름(`"UE5-Source-5.8"`)이면 소스빌드이므로 `HKCU`의 `Builds`를 봅니다.
**팀원은 런처, 저는 소스빌드**라는 차이가 여기서 갈립니다.

한 번 해석에 성공하면 `Developers/BuildConfig.ini`에 적어두고 다음부터는 그것을
씁니다. 이 디렉토리는 `.gitignore`에 들어 있어서 개인 파일로 남고, 팀 공용 파일에는
아무 경로도 들어가지 않습니다.

결과는 종료 코드로 구분해서 AI가 다음 행동을 정할 수 있게 했습니다 — `0` 성공,
`2` `.uproject`를 못 찾음, `3` 엔진 경로나 `Build.bat` 문제, `4` 에디터가 켜져 있어
DLL이 잠김. 실패하면 UBT 출력에서 `error` 라인만 추려 보여줍니다.

이 스킬은 `finish-feature`의 PR 체크리스트 단계에서 호출되고, `exit 0`이 아니면
PR 단계로 넘어가지 않습니다. "빌드 깨진 채 push 금지" 규칙이 실제로 강제되는
지점입니다.

### 그 밖에 얹은 것들

빌드 검증 외에도 에이전트가 헛돌지 않게 하는 장치를 몇 가지 넣었습니다. 프로젝트
구조를 설명하는 `CLAUDE.md`와 아키텍처 문서, 팀이 쓰는 도메인 용어를 고정한
`glossary.md`, 에디터를 직접 조작할 수 있게 한 UE MCP 툴셋과 에디터 Python 원격
실행 설정입니다.

**판단 근거를 남기는 `devlog` 시스템은 제가 만든 것이 아니라 팀원이 만든
것입니다.** 저는 그 시스템의 사용자로 항목 5건을 작성했고, 같은 날짜에 ID가
충돌하는 문제를 발견해 재발번 규칙을 고친 정도로 기여했습니다.

## 장단점

- **장점 — 팀원이 아무 설정도 하지 않아도 됩니다.** 경로를 묻지도, 문서를 고치지도
  않고 스킬을 그냥 부르면 됩니다. 런처 설치든 소스빌드든 같은 명령으로 돌아갑니다.
- **장점 — 개인 환경 정보가 팀 공용 파일에서 빠졌습니다.** 애초에 이걸 만든 이유이기도
  합니다. 경로는 `.gitignore`된 개인 파일에만 남습니다.
- **장점 — 규칙이 실행 가능한 형태가 됐습니다.** 문서의 체크박스로 남아 있던 항목이
  종료 코드로 판정되는 통과 조건이 됐습니다.
- **단점 — AI를 쓰지 않고 직접 커밋·푸시하는 사람은 이 기능을 쓸 수 없습니다.**
  게이트가 에이전트 워크플로우 안에만 있어서, 스킬을 부르지 않으면 아무것도 검사되지
  않습니다. 규칙 자체를 강제하는 장치는 아닙니다.
- **단점 — 컴파일만 봅니다.** 빌드가 통과했다는 것은 문법이 맞다는 뜻이지 코드가 맞다는
  뜻이 아닙니다. AI가 논리적으로 틀린 코드를 짜도 이 단계는 통과합니다.
- **단점 — 4단계 폴백은 실제 팀원 환경에서 검증하지 않았습니다.** 팀원 PC에서 빌드가
  깨진 적이 있어서 만든 게 아니라 미리 대비한 것이라, 레지스트리 분기가 남의
  환경에서 실제로 맞는지는 확인되지 않았습니다.
- **단점 — 에디터가 켜져 있으면 사람이 개입해야 합니다.** DLL 잠금 때문에 `exit 4`로
  멈추고, 에디터를 닫는 것은 사람이 해야 합니다.

## 대안 비교

|  | A. CLAUDE.md에 엔진 경로 명시 | B. PIE·자동화 테스트까지 검증 | 선택한 방식 |
|---|---|---|---|
| 방식 | 팀 공용 문서에 경로 한 줄을 적어두고 AI가 그것을 읽음 | 컴파일에 더해 에디터 실행·PIE 크래시·자동화 테스트까지 확인 | 스킬 안에서 4단계로 경로를 자동 해석하고 컴파일만 검증 |
| 팀원 대응 | 불가 — 경로가 사람마다 다르고, 런처/소스빌드 차이까지 있음 | 해당 없음 | 런처·소스빌드를 레지스트리에서 갈라 처리 |
| 비용 | 없음 | 토큰 소모가 클 것으로 예상 | 스크립트 유지보수 |
| 실측 | 측정 안 함 | 측정 안 함 | 측정 안 함 |

**A는 실제로 한 번 그렇게 됐다가 버린 방식입니다.** AI가 엔진 경로를 찾아낸 뒤 그
값을 팀 공용 `CLAUDE.md`에 적어놨고, 그것을 보고 이 스킬을 만들게 됐습니다.

**B는 의도적으로 범위에서 뺐습니다.** 에디터 실행이나 PIE 크래시 판정은 어차피 AI가
제대로 해낼 수 있는 영역이 아니라고 봤고, 해낸다 해도 소모하는 토큰이 막대할 것으로
예상했습니다. 애초 목적이 "커밋 규칙을 자동으로 준수하게 만드는 것"이었기 때문에,
규칙에 적힌 빌드 통과 여부만 확실히 판정하는 쪽으로 좁혔습니다. 그래서 `SKILL.md`에
에디터 실행·PIE·자동화 테스트·패키징은 하지 않는다고 못 박아두었습니다.

도입 이후 빌드가 깨진 채 push 되는 일이 줄었는지는 **측정하지 않았습니다.**

## 회고

지금 다시 만들어도 비슷하게 만들 것 같습니다. 다만 **AI를 쓰지 않고 직접 커밋·푸시하는
사람은 이 기능을 쓸 수 없다는 단점**이 남습니다. 규칙을 강제하는 더 확실한 방법이
따로 있는지 찾아보면 좋겠다고 생각합니다.
