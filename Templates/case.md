---
id: 파일명과-동일한-slug
title: 성과가 드러나는 한 줄 제목 (숫자 포함 권장)
status: draft                       # draft | review | ready
summary: 한 줄 요약. 목차와 JD 매칭 근거에 그대로 쓰인다. 대표 숫자를 포함할 것
project: 프로젝트명
role: 맡은 역할
period: YYYY-MM ~ YYYY-MM (기간)
engine: []                          # 예: [Unity, URP] / [Unreal] / [자체엔진]
lang: []                            # 예: [C#, HLSL] / [C++]
domain: []                          # JD 매칭 1차 축. 예: [렌더링, 최적화, 네트워크, 게임플레이, 툴]
skills: []                          # JD 매칭 2차 축. 예: [프로파일링, 셰이더, 배칭, 멀티스레딩]
sources:                            # 출처. 파일 경로 또는 URL. 비워두지 말 것
  - Sources/파일명.pdf p.0
confidence: medium                  # high | medium | low — 압박 면접에서 방어 가능한 정도
pages: 2                            # 예상 분량(페이지). 빌드 시 총량 관리용
---

<!--
  작성 규칙 (build.py가 검사하는 것 + 사람이 지켜야 하는 것):
  1. 소스에 없는 수치·기술을 지어내지 않는다. 모르면 Sources를 확인하거나 사용자에게 묻는다.
  2. '접근' 섹션에는 반드시 검토했다가 탈락시킨 대안을 최소 1개 적는다. 이게 없으면 review-case를 통과하지 못한다.
  3. '결과'의 숫자는 이 문서 전체에서 유일한 출처다. summary·title에 같은 숫자를 옮겨 적을 때는
     이 섹션 기준으로 맞춘다. frontmatter에 숫자를 별도로 적지 않는다.
  4. status: ready 로 직접 바꾸지 않는다. /review-case 통과 후에만 승격한다.
  5. 말투는 '-습니다'체로, 동료에게 설명하듯 친절하게 쓴다. "~있었다/~였다" 같은
     딱딱한 보고서체(-다체)는 쓰지 않는다.
  6. 항목을 억지로 채우지 않는다. 쓸 내용이 없으면 비워두고 사용자에게 정보를
     요청한다. 매 섹션을 채우는 것보다 채운 내용이 전부 사실인 것이 우선이다.
  7. '결과' 섹션에는 측정된 데이터(수치 + 필요한 측정 조건: 기기·씬·반복 횟수)만
     쓴다. 그 수치를 어떻게 측정했는지(테스트 코드, 임시로 바꿔본 방법 등)는
     본문 어디에도 쓰지 않는다.
  8. Sources/Portfolios.md에 이 사례와 관련된 이미지(스크린샷·다이어그램·그래프)가
     있으면 적극 활용한다. `Assets/<case-id>_설명.png`로 받아 저장하고
     `![설명](../Assets/<파일명>)` 형식으로 관련 섹션에 삽입한다.
-->

## 상황

무엇이 문제였는가. 왜 이것이 해결할 가치가 있었는가. 2~3문장.

## 제약

타깃 기기, 일정, 기존 코드베이스, 팀 상황 등 문제의 난이도를 결정한 조건.

## 접근

검토한 후보안 2~3개와, 그중 선택하지 않은 것들을 버린 이유.

- **안 A** — 설명. 버린 이유: ...
- **안 B** — 설명. 버린 이유: ...
- **선택한 안** — 설명. 선택 이유: ...

## 구현

핵심 구조. 필요하면 20줄 이내 코드 스니펫 하나.

```
// 코드는 짧고 핵심만
```

## 결과

측정 조건을 명시한 숫자. (기기, 씬, 반복 횟수 등)

- 지표: 값 A → 값 B (측정 조건)

## 회고

지금 다시 한다면 무엇을 다르게 할 것인가. 한계나 트레이드오프를 인정하는 문장 포함.
